"""Local development instance of the book viewer.

Runs the server on localhost with its own archive/cache (``dev/archive``,
``dev/cache``) so the production Docker container is never touched. See
``dev/run.py`` (``python -m dev.run``) for the entry point.
"""
